/**
 * Message Tree Constants
 * Story 25.4: Server — Branch-aware Message Pagination
 */

// Special key for root-level branch selection (multi-root from compact_boundary)
export const ROOT_BRANCH_KEY = '__root__' as const;
